<Window x:Class="TeacherPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeacherPlanner"
        xmlns:model="clr-namespace:TeacherPlanner.Models"
        xmlns:viewmodel="clr-namespace:TeacherPlanner.ViewModels"
        xmlns:view="clr-namespace:TeacherPlanner.Views"
        mc:Ignorable="d"
        d:DataContext="{Binding Source={d:DesignInstance Type=viewmodel:MainViewModel}}"
        Title="Teacher Planner" Height="1080" Width="1920" Name="mainwindow">

    <TabControl>



        <TabItem Header="Planner">
            <view:PlannerPageView DataContext="{Binding PlannerViewModel}"/>
        </TabItem>
        <TabItem Name="Todo" Header="Todo">
            <view:TodoPageView DataContext="{Binding ToDoViewModel}"/>
        </TabItem>
    </TabControl>
</Window>
